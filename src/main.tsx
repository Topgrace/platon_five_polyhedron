import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import PolyNetSliderApp from "./poly-net-slider-app";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PolyNetSliderApp />
  </React.StrictMode>,
);

